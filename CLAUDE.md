# Kaisola project instructions

## Backlog check
- At the start of any Kaisola task — and again before wrapping up a work session — read `docs/BACKLOG.md` for new or updated items. Fold anything relevant into the current plan, and explicitly surface newly discovered backlog items to Michael instead of silently skipping them. Treat `docs/BACKLOG.md` as the authoritative list of pending product asks.
