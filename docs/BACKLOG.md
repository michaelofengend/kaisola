# Backlog

- [x] See this screenshot, I'd like to make it more readable, perhaps make the terminal text less, dense, easy to read, more spacisious? but also smaller? better font?

  ![Terminal readability reference](<backlog-media/Screenshot 2026-07-14 at 11.24.50 AM.png>)

- [x] when going from edit to preview eye symbol the image screenshot from md file disappears from edit mode?  Also when there's more than one screeenshot it seems that the other one dissapears, gets deleted, actually they all get deleted.. Make sure visuals in MD file keep appearing, we really should make MD file editing like google docs/less-wrong/substack, simple, easy, minamilist and easy configurable and visual. 

  ![Markdown image-loss example](<backlog-media/Screenshot 2026-07-14 at 7.54.50 PM.png>)

- [x] also - would like to implement such that kaisola remembers sessions of windows, say I close the window, and then open the window app later on, perhaps a day or a week or something it should remember the sessions unless I delete the window (double click option for thus).

- [x] I want history to automatically appear during agent chats when you scroll up, like an infinite scroll vibes so that you don't have to keep clicking on load more chat history!

  

- [x] make the onboarding beads run away from cursor now towards, and make letters more dense on the rows, and make it more smooth if possible

- [x] Also when adding terminal or new window side tabs, it would be nice if it automatically knew how to be space efficient in the way that if there'es two tabs it opens them vertically stacked like this: 

  

- [x] sessions should be movable/orderable in different ways as well. also when moving window tabs it gives the green plus button, when moving them it should feel more intuitive (if we move them around or add/drop them elsewhere)

  

- [x] horizontal tabs should maximizable/minamiziable formable as well, 

- [x] also it seems the approve plan and create isolated worktrees doesn't work well for mesh?? Large frozen patches now route through an immutable local Git review instead of blocking integration, and an interrupted older review exposes a retry action.
